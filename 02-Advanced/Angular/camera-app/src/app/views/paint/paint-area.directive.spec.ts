import { PaintAreaDirective } from "./paint-area.directive";

describe("PaintAreaDirective", () => {
  it("should create an instance", () => {
    const directive = new PaintAreaDirective();
    expect(directive).toBeTruthy();
  });
});
